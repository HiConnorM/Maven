{
    "compilerOptions": {
      "target": "ES2016",                            // Target ECMAScript 2016 for compatibility.
      "module": "commonjs",                          // Use CommonJS for Node.js modules.
      "outDir": "./dist",                            // Compiled JavaScript files will be in the "dist" directory.
      "rootDir": "./src",                            // TypeScript files will be inside the "src" directory.
      "strict": true,                                // Enable strict type-checking options.
      "esModuleInterop": true,                       // Ease importing CommonJS modules (e.g., require() style imports).
      "forceConsistentCasingInFileNames": true,      // Enforce consistent casing in imports.
      "skipLibCheck": true                           // Skip type-checking .d.ts files for faster compilation.
    },
    "include": ["src/**/*"],                          // Include all files in the "src" directory.
    "exclude": ["node_modules", "dist"]               // Exclude node_modules and build output directory.
  }
  